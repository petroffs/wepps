meta {
  name: GET users.item
  type: http
  seq: 2
}

get {
  url: {{base_url}}/rest/v1/users.item
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
  id: 1
}

docs {
  Get single user by id.
  Доступно только для M2M клиентов.
}
