meta {
  name: GET profile
  type: http
  seq: 1
}

get {
  url: {{base_url}}/rest/v1/profile
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Get current user profile.
  Требует авторизации (Bearer token).
}
