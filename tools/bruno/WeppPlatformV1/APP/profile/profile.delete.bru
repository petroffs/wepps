meta {
  name: DELETE profile
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/rest/v1/profile
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Delete current user account.
  Требует авторизации.
}
