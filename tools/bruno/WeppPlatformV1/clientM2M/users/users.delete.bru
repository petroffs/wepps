meta {
  name: DELETE users
  type: http
  seq: 5
  tags: destructive manual
}

delete {
  url: {{base_url}}/rest/v1/users?id={{user_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Delete user by id.
  Доступно только для M2M клиентов.
}
