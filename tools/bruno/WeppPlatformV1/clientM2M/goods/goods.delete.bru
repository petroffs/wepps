meta {
  name: DELETE goods
  type: http
  seq: 6
  tags: destructive manual
}

delete {
  url: {{base_url}}/rest/m2m/products?id={{product_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Delete product/goods item by id.
  Требует роль UserPermissions = 1 или 2.
}
