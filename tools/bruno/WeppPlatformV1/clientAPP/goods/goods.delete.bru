meta {
  name: DELETE goods
  type: http
  seq: 9
  tags: destructive manual
}

delete {
  url: {{base_url}}/rest/v1/goods?id=1
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
  id: 1
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Delete goods item by id.
  Требует роль UserPermissions = 1 или 2.
  
  id — идентификатор товара для удаления (обязательно).
}
