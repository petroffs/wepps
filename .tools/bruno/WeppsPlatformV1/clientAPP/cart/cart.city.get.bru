meta {
  name: GET cart.city
  type: http
  seq: 2
}

get {
  url: {{base_url}}/rest/v1/cart.city?q=Мос
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("data is array", function() {
    expect(res.body.data).to.be.an("array");
  });
}
