meta {
  name: PUT goods.attributesValues
  type: http
  seq: 3
}

put {
  url: {{base_url}}/rest/m2m/goods.attributesValues
  body: json
  auth: inherit
}

body:json {
  {
    "data": [
      {
        "id": 6,
        "name": "Бренд-6",
        "priority": 50,
        "group": 1
      }
    ]
  }
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
