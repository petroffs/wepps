meta {
  name: DELETE goods.images
  type: http
  seq: 21
}

delete {
  url: {{base_url}}/rest/m2m/goods.images
  body: none
  auth: inherit
}

params:query {
  id: 1
}


tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
