meta {
  name: GET goods.stocks
  type: http
  seq: 10
}

get {
  url: {{base_url}}/rest/m2m/goods.stocks?page=1&limit=100
  body: none
  auth: inherit
}

params:query {
  page: 1
  limit: 100
  ~goodsId: {{goods_id}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
