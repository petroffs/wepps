meta {
  name: GET goods.attributes
  type: http
  seq: 1
}

get {
  url: {{base_url}}/rest/m2m/goods.attributes
  body: none
  auth: inherit
}

params:query {
  ~page: 1
  ~limit: 100
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("data is array", function() {
    expect(res.body.data).to.be.an("array");
  });
}

docs {
  GET /rest/m2m/goods.attributes — свойства товаров (s_Properties).
  Доступно только для M2M клиентов.
  
  Параметры (опциональные): page, limit
  
  Возвращает список свойств: id, guid, name, alias, priority, pGroup.
  Сортировка по Priority asc.
}
