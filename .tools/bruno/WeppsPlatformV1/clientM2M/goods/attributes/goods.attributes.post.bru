meta {
  name: POST goods.attributes
  type: http
  seq: 3
}

put {
  url: {{base_url}}/rest/m2m/goods.filters
  body: json
  auth: inherit
}

body:json {
  {
    "data": {
      "id": 1,
      "name": "Size",
      "priority": 5,
      "is_hidden": 0
    }
  }
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
