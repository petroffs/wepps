meta {
  name: DELETE orders
  type: http
  seq: 6
}

delete {
  url: {{base_url}}/rest/m2m/orders
  body: json
  auth: inherit
}

body:json {
  {
    "data": [
      2
    ]
  }
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Delete order by id.
  Доступно только для M2M клиентов.
}
