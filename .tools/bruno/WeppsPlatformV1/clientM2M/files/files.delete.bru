meta {
  name: DELETE files
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/rest/m2m/goods.images
  body: json
  auth: inherit
}

body:json {
  {
    "data": [
        -1
    ]
  }
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
