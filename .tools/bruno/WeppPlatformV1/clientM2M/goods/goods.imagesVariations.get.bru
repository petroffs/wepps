meta {
  name: GET goods.imagesVariations
  type: http
  seq: 8
}

get {
  url: {{base_url}}/rest/m2m/goods.imagesVariations?goods_id=326
  body: none
  auth: bearer
}

params:query {
  goods_id: 326
  ~page: 1
  ~limit: 100
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
