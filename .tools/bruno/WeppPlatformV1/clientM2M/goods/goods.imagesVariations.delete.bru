meta {
  name: DELETE goods.imagesVariations
  type: http
  seq: 20
}

delete {
  url: {{base_url}}/rest/m2m/goods.imagesVariations
  body: none
  auth: bearer
}

params:query {
  id: 1
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
