meta {
  name: GET goods.prices
  type: http
  seq: 6
}

get {
  url: {{base_url}}/rest/m2m/goods.prices?page=1&limit=1
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 1
  ~goods_id: 327
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
