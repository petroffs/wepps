meta {
  name: GET goods.images
  type: http
  seq: 7
}

get {
  url: {{base_url}}/rest/m2m/goods.images
  body: none
  auth: bearer
}

params:query {
  ~goods_id: 
  ~page: 1
  ~limit: 100
}

auth:bearer {
  token: {{access_token}}
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}
