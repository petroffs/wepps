meta {
  name: DELETE orders
  type: http
  seq: 6
  tags: destructive manual
}

delete {
  url: {{base_url}}/rest/m2m/orders?id={{order_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Delete order by id.
  Доступно только для M2M клиентов.
}
