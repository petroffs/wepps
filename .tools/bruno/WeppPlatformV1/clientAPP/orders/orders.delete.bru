meta {
  name: DELETE orders
  type: http
  seq: 5
  tags: destructive manual
}

delete {
  url: {{base_url}}/rest/v1/orders?id={{order_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
}

tests {
  test("status 200", function() {
    expect(res.status).to.equal(200);
  });
}

docs {
  Cancel order by id.
  Требует авторизации. Параметр id обязателен.
}
